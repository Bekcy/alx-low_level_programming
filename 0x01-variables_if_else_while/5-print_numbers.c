#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main ->assign random number to the variable n each time executed
*and prints based on a condition
*Return: always 0
*/
int main(void)
{
int n;
for (n = 0; n < 10; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
