#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main ->asiign a random number to the variable n each time executed
*and prints the number stored in n
*Return: always 0
*/
int main(void)
{
int ch;
char A;
for (ch = 0; ch <= 9; ch++)
{
putchar(ch + 48);
}
for (A = 'ch'; A = 'f'; A++)
{
putchar(A);
}
putchar("\n");
return (0);
}
